#include "main.h"

/**
 * print_numbers - function to print the numbers from 0 to 9
 * Return: 0 fro success
 */

void print_numbers(void)
{
int a;

if (a >= 0 && a <= 9)
{
_putchar(a + '0');
_putchar('\n');
}
}
